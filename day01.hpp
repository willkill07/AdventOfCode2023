#include <string_view>

auto
parse(auto scheduler, std::string_view const input)
{
  return input;
}

auto
part1(auto scheduler, auto const& parsed)
{
  return 0;
}

auto
part2(auto scheduler, auto const& parsed, auto const& part1_answer)
{
  return parsed[0];
}
